// 変数の宣言・値の代入：体重
let userWeight = 68;

// 変数の宣言・値の代入：身長
let userHeight = 1.7;

// 体重(kg)]÷[身長(m)×身長(m)]
let userBmi = userWeight/(userHeight * userHeight);

//  コンソールへの出力
console.log(userBmi);
